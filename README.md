This is a look alike youtube web application develped using react-redux.


Install all the dependencies of the project using `npm install` after cloning the project.
Then use the command `npm start` to run it. 
